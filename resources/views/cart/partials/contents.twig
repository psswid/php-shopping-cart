<table class="table">
  {% for item in basket.all %}
    <tr>
      <td>Product</td>
      <td>Quantity</td>
    </tr>
    <tr>
      <td>{{ item.title }}</td>
      <td>{{ item.quantity }}</td>
    </tr>
  {% endfor %}
</table>
